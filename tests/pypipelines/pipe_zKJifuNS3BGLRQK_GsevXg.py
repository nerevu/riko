# Pipe pipe_zKJifuNS3BGLRQK_GsevXg generated by pipe2py

from riko import Context
from riko.modules.fetch import pipe as fetch
from riko.modules.split import pipe as split
from riko.modules.count import pipe as count
from riko.modules.simplemath import pipe as simplemath
from riko.modules.simplemath import pipe as simplemath
from riko.modules.truncate import pipe as truncate


def pipe_zKJifuNS3BGLRQK_GsevXg(context=None, _INPUT=None, conf=None, **kwargs):
    # todo: insert pipeline description here
    conf = conf or {}

    if context and context.describe_input:
        return []

    if context and context.describe_dependencies:
        return [
            "pipecount",
            "pipefetch",
            "pipeoutput",
            "pipesimplemath",
            "pipesplit",
            "pipetruncate",
        ]

    sw_224 = fetch(
        context,
        conf={
            "URL": {
                "type": "url",
                "value": "file://data/www.sciencedaily.com_rss_computers_math.html",
            }
        },
    )

    sw_250 = split(context, sw_224, splits=2, conf=None)

    sw_243 = count(context, sw_250, conf=None)

    sw_94 = simplemath(
        context,
        sw_243,
        conf={
            "OTHER": {"type": "number", "value": "5"},
            "OP": {"type": "text", "value": "modulo"},
        },
    )

    sw_169 = simplemath(
        context,
        sw_243,
        OTHER=sw_94,
        conf={
            "OTHER": {"terminal": "OTHER", "type": "number"},
            "OP": {"type": "text", "value": "subtract"},
        },
    )

    sw_232 = truncate(
        context,
        sw_250,
        count=sw_169,
        conf={"count": {"terminal": "count", "type": "number"}},
    )

    return sw_232


if __name__ == "__main__":
    pipeline = pipe_zKJifuNS3BGLRQK_GsevXg(Context())

    for i in pipeline:
        print i
