# -*- coding: utf-8 -*-
# vim: sw=4:ts=4:expandtab
# Pipe pipe_testpipe1 generated by riko

from riko import Context
from riko.modules.fetch import pipe as fetch
from riko.modules.filter import pipe as _filter
from riko.types.general import Conf
from riko.types.modules import FetchRawConf, FilterRawConf


def pipe_testpipe1(
    item=None, conf: Conf = None, context: Context | None = None, **kwargs
):
    if context and context.describe_input:
        result = []
    elif context and context.describe_dependencies:
        result = ["fetch", "filter"]
    else:
        sw_90 = fetch(
            None,
            conf=FetchRawConf(
                {"url": {"type": "url", "value": "file://riko/data/feed.xml"}}
            ),
            context=context,
        )
        sw_102 = _filter(
            sw_90,
            conf=FilterRawConf(
                {
                    "combine": {"type": "text", "value": "and"},
                    "permit": {"type": "bool", "value": True},
                    "rule": [
                        {
                            "field": {"type": "text", "value": "description"},
                            "op": {"type": "text", "value": "contains"},
                            "value": {"type": "text", "value": "the"},
                        }
                    ],
                }
            ),
            context=context,
        )
        _OUTPUT = sw_102
        result = _OUTPUT

    return result


if __name__ == "__main__":
    for i in pipe_testpipe1():
        print(i)
