# Pipe pipe_402e244d09a4146cd80421c6628eb6d9 generated by pipe2py

from riko import Context, get_path
from riko.modules.loop import pipe as loop
from riko.modules.fetchdata import pipe as fetchdata
from riko.modules.rename import pipe as rename
from riko.modules.regex import pipe as regex


def pipe_402e244d09a4146cd80421c6628eb6d9(context=None, conf=None):
    # todo: insert pipeline description here
    conf = conf or {}

    if context and context.describe_input:
        return []

    if context and context.describe_dependencies:
        return ["fetchdata", "regex", "rename"]

    sw_572 = fetchdata(
        context=context,
        conf={
            "URL": {
                "type": "url",
                "value": get_path("www.bbc.co.uk_programmes_b006mvlc_episodes_player.xml"),
            },
            "path": {"type": "text", "value": "episode"},
        },
    )

    sw_587 = loop(
        sw_572,
        embed=rename,
        context=context,
        conf={
            "count": "all",
            "embed": {
                "emit": True,
                "conf": {
                    "RULE": [
                        {
                            "field": {"type": "text", "value": "programme.title"},
                            "copy": {"type": "bool", "value": True},
                            "newval": {"type": "text", "value": "title"},
                        },
                        {
                            "field": {"type": "text", "value": "programme.pid"},
                            "copy": {"type": "bool", "value": True},
                            "newval": {"type": "text", "value": "link"},
                        },
                        {
                            "field": {"type": "text", "value": "programme.short_synopsis"},
                            "copy": {"type": "bool", "value": True},
                            "newval": {"type": "text", "value": "description"},
                        },
                    ]
                },
            },
        },
    )

    sw_598 = loop(
        sw_587,
        embed=regex,
        context=context,
        conf={
            "count": "all",
            "embed": {
                "emit": True,
                "conf": {
                    "RULE": [
                        {
                            "field": {"type": "text", "value": "link"},
                            "match": {"type": "text", "value": "(.*)"},
                            "replace": {
                                "type": "text",
                                "value": "http://www.bbc.co.uk/programmes/$1",
                            },
                        }
                    ]
                },
            },
        },
    )

    return sw_598


if __name__ == "__main__":
    pipeline = pipe_402e244d09a4146cd80421c6628eb6d9(Context())

    for i in pipeline:
        print(i)
