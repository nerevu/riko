# Pipe pipe_QMrlL_FS3BGlpwryODY80A generated by pipe2py

from riko import Context, get_path
from riko.modules.fetch import pipe as fetch
from riko.modules.split import pipe as split
from riko.modules.filter import pipe as _filter
from riko.modules.regex import pipe as regex
from riko.modules.filter import pipe as _filter
from riko.modules.regex import pipe as regex
from riko.modules.union import pipe as union


def pipe_QMrlL_FS3BGlpwryODY80A(context=None, _INPUT=None, conf=None, **kwargs):
    # todo: insert pipeline description here
    conf = conf or {}

    if context and context.describe_input:
        return []

    if context and context.describe_dependencies:
        return [
            "fetch",
            "filter",
            "regex",
            "split",
            "union",
        ]

    sw_140 = fetch(
        context=context,
        conf={
            "URL": {"type": "url", "value": get_path("news.yahoo.com_rss_health.xml")}
        },
    )

    sw_108 = split(sw_140, context=context, splits=2, conf=None)

    sw_159 = _filter(
        sw_108,
        context=context,
        conf={
            "COMBINE": {"type": "text", "value": "and"},
            "EMIT": {"type": "bool", "value": True},
            "RULE": {
                "field": {"type": "text", "value": "description"},
                "value": {"type": "text", "value": "drug"},
                "op": {"type": "text", "value": "contains"},
            },
        },
    )

    sw_204 = regex(
        sw_159,
        context=context,
        conf={
            "RULE": {
                "field": {"type": "text", "value": "title"},
                "match": {"type": "text", "value": "(.+)"},
                "replace": {"type": "text", "value": "[Drugs] $1"},
            }
        },
    )

    sw_148 = _filter(
        sw_108,
        context=context,
        conf={
            "COMBINE": {"type": "text", "value": "and"},
            "EMIT": {"type": "bool", "value": True},
            "RULE": {
                "field": {"type": "text", "value": "description"},
                "value": {"type": "text", "value": "weight"},
                "op": {"type": "text", "value": "contains"},
            },
        },
    )

    sw_189 = regex(
        sw_148,
        context=context,
        conf={
            "RULE": {
                "field": {"type": "text", "value": "title"},
                "match": {"type": "text", "value": "(.+)"},
                "replace": {"type": "text", "value": "[Weight] $1"},
            }
        },
    )

    sw_170 = union(context, _OTHER3=sw_204, conf=None, _OTHER=sw_189)

    return sw_170


if __name__ == "__main__":
    pipeline = pipe_QMrlL_FS3BGlpwryODY80A(Context())

    for i in pipeline:
        print(i)
